import { makeStyles } from "@material-ui/core";

export const detailsStyles = makeStyles(() => ({
  
    image: {
        height: "380px",
    },
    closeIcon: {
        float: "right",
        position: 'absolute',
        right:20,
    }
  }));